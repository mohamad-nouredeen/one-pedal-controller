# Simulink Models

This folder contains the main Simulink and Stateflow models developed for the One-Pedal Controller project.

The models cover the controller logic, longitudinal vehicle dynamics, closed-loop simulation, and functional-safety testing.

## Folder Structure

```text
models/
├── controller/
│   └── controller_final.slx
├── plant/
│   └── plant_final.slx
├── harness/
│   └── harness_final.slx
└── safety/
    └── controller_unit_integration_tests_final.slx